import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.beledarian.markdowneditor',
  appName: 'Beledarians Markdown Editor',
  webDir: 'dist'
};

export default config;
